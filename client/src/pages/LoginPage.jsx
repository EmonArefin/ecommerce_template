import React from 'react';
import LoginForm from '../component/loginComponent/Login';

const LoginPage = () => {
    return (
        <div>
            <LoginForm/>
        </div>
    );
};

export default LoginPage;